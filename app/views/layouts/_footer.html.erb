        <div class="<%= common_sizing %> mb-3 bg-red3 rounded-bottom d-flex justify-content-center py-1">
        <a href="mailto:alan@alanvardy.com" class="btn btn-sm btn-outline-grey0 mx-4"><i class="fas fa-envelope mr-1"></i> Email</a>
        <% if !admin? %>
            <%= link_to(session_new_path, html_options = { class: "btn btn-sm btn-outline-grey0" }) do %>
                <i class="fas fa-lock mr-1"></i> Admin
            <% end %>
        <% end %>
        <a href="https://www.alanvardy.com/" class="btn btn-sm btn-outline-grey0 mx-4"><i class="fas fa-sitemap mr-1"></i> Site</a>
        </div>